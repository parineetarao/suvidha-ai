{# Fallback for any scheme without a bespoke letter — inherits base.j2's
   header/body/footer verbatim. This is the template every newly-onboarded
   scheme gets by default until someone writes scheme-specific wording. #}
{% extends "base.j2" %}
